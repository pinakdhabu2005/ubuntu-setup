# ubuntu-setup
all required setup for sppu practicals and more stuff

base
install VSCode
sudo apt install curl -y
curl -fsSL https://is.gd/vscodeubuntu | sudo sh

python DSBDA all setup
curl -fsSL is.gd/dsbdaubuntu | sudo bash
install google chrome
curl -fsSL https://is.gd/pinakchrome  | sudo sh


extra
curl -fsSL is.gd/vscodelinux | sh
